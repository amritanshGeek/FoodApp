import { View, Text } from 'native-base';
import React, { FC, memo } from 'react';

/**
 * Home
 */
const Home: FC = () => {
  return (
    <View>
      <Text>Home page </Text>
    </View>
  );
};

export default memo(Home);
