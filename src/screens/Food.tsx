import { View, Text } from 'native-base';
import React, { FC, memo } from 'react';

/**
 * Food
 */
const Food: FC = () => {
  return (
    <View>
      <Text>Food page </Text>
    </View>
  );
};

export default memo(Food);
