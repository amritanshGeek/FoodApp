import { View, Text } from 'native-base';
import React, { FC, memo } from 'react';

/**
 * Cart
 */
const Cart: FC = () => {
  return (
    <View>
      <Text>Cart page </Text>
    </View>
  );
};

export default memo(Cart);
